@model IEnumerable<OnlineExamService.Models.McqAnswer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.McqAnswers)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.McqAnswers)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.McqAnswerId }) |
            @Html.ActionLink("Details", "Details", new { id=item.McqAnswerId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.McqAnswerId })
        </td>
    </tr>
}

</table>
